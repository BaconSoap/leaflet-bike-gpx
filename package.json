{
  "name": "leaflet2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "make": "node node_modules/.bin/tsc --target ES5 --emitDecoratorMetadata --out build/app.js src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "python -m SimpleHTTPServer",
    "watch": "supervisor -w src/ -e ts -n exit -x 'npm' -- run make -s"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^1.5.0-beta"
  },
  "dependencies": {
    "reflect-metadata": "^0.1.0"
  }
}
